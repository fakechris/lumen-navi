Single-line text field on a subtle warm fill with a strong 1px border; focus brings the accent. Pass `icon` for a search-style field.

```jsx
<Input placeholder="Project name" />
<Input icon="search" placeholder="Search transcripts…" />
<Input invalid defaultValue="bad@" />
```
