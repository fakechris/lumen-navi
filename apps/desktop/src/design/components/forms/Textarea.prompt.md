Multi-line field, vertically resizable. Use `reading` when editing long-form transcript/translation text — it switches to the serif reading face at reading size.

```jsx
<Textarea placeholder="Notes…" rows={3} />
<Textarea reading defaultValue="So the first thing we shipped was…" />
```
