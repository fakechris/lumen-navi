Styled native `<select>` with the family chevron. Pass `options` as strings or `{value,label}`.

```jsx
<Select options={["SenseVoice", "Whisper"]} />
<Select options={[{value:"zh",label:"中文"},{value:"en",label:"English"}]} style={{width:180}} />
```
