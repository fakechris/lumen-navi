Borderless spinner in `currentColor` — drop it inside a button, notice, or toolbar and it takes that context's color.

```jsx
<Button variant="secondary"><Spinner size={13} /> Working…</Button>
<Spinner />
```
