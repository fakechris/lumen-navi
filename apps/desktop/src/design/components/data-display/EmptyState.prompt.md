Guidance block that replaces a blank list — text-first with a quiet icon and an optional action. Every list surface in the family shows one rather than empty space.

```jsx
<EmptyState icon="folder" title="No projects yet"
  action={<Button icon="upload">Import media</Button>}>
  Drop an audio or video file to start your first transcript.
</EmptyState>
```
