Sidebar or top-bar navigation entry — icon + label, active state raises a surface fill. Renders as `<a>` with `href`, else `<button>`.

```jsx
<NavItem icon="transcript" label="Projects" active />
<NavItem icon="settings" label="Settings" onClick={go} />
<NavItem icon="search" label="Search" href="#/search" />
```
